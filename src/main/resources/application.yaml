eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8762
spring:
  application:
    name: zuul-server
zuul:
  ignored-services: '*'
  routes:
    gallery-service:
      path: /gallery/**
      service-id: gallery-service
